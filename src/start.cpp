// Main file.

// ctx/global-context.hpp immediatley initializes thread-safe global context variables and starts the GC communication thread
#include "src/ctx/global-context.hpp"

auto
main()
-> int {

  // ...

  // Exit
  return 0;
}
